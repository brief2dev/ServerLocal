version: "3.9"

services:
  webs-company:
    build: .
    restart: unless-stopped
    volumes:
      - ./sites:/var/www/html
      - ./apache/000-default.conf:/etc/apache2/sites-enabled/000-default.conf
    networks:
      - infra_net

networks:
  infra_net:
    external: true